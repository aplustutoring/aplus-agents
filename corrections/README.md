# corrections/

The agent feedback log. Captures human corrections to agent output (voice, facts,
brand) so the fleet can learn from them over time.

Empty for now (`.gitkeep` placeholder). Each correction should be one record:
what the agent produced, what was wrong, and the corrected version.
