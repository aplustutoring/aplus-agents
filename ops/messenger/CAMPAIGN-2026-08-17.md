# Charter 26/27 re-engagement launch — Monday 2026-08-17, 9:00 AM PT

Segmented outreach to the charter gap audience (Roman 2026-08-14: families we
worked with last year, families we didn't, and the same split for teachers).
All segments derive from gap list 3104 (429 families, corrected cutoff) and
the Family→TOR associations.

## Segments (HubSpot static lists, built 2026-08-14)

| List | Segment | Count | Angle |
|---|---|---|---|
| 3107 | Families — Hot (invoiced ≤90d) | 12 | "Don't lose momentum" — {{contact.student_first_name}} + tutor {{contact.last_tutor_name}} |
| 3108 | Families — Win-back (invoiced, lapsed) | 387 | "{{contact.student_first_name}}'s tutor {{contact.last_tutor_name}} misses them" |
| 3109 | Families — Never Started (deal, no invoice) | 30 | "Your funds were approved but tutoring never started" (NO student/tutor tokens — not stamped) |
| 3110 | TORs — Worked With Us | 163 | Partner memo: your families can restart; reply for a roster |
| 3111 | TORs — New Outreach | 8 | Intro: we're an approved vendor; here's how families use funds with us |

Data note: 329/429 gap families have a TOR association; 100 have none
(hygiene queue candidate).

## The workflow (per family segment)

Enrollment is API-driven Monday morning (below) — workflows need NO
enrollment trigger; goal/exit does the safety work.

- Day 0 (Mon 9am): segment email 1
- Day 3 (Thu): email 2 (only if no reply/renewal)
- Day 7: task for CHARTER SALES role (call top-of-list by total invoiced)
- GOAL / auto-exit: contact gets a charter deal created ≥ 2026-08-01
  (= renewed) or replies. Suppression: HubSpot-native (opt-out, bounce,
  non-marketing) at each send.

TOR segments: Day 0 email only, Day 5 follow-up for 3110 (no task step).

## Launch mechanics

1. Emails: drafts in `templates/campaign-2026-08-17/` (one .md per segment,
   subject + body, HubSpot personalization tokens). After Roman approves the
   copy they become AUTOMATED marketing emails in the portal (built by agent
   or pasted by Roman — agent needs a branded base email id to clone).
2. Workflows: 5 shells built in HubSpot per the cadence above, OFF until
   emails are attached; ids go into `campaign.yml`.
3. Monday 9:00 AM PT: `.github/workflows/monday-launch.yml` (cron, one-shot,
   gated) runs `enroll.py` — enrolls each list into its workflow via the
   automation API. Runs ONLY when `campaign.yml` has `armed: true` AND
   today == `launch_date`; logs per-contact results; disarms are a one-line
   config change.

## Before Monday (Roman)

- [ ] Approve/edit the 5 copy drafts (templates/campaign-2026-08-17/)
- [ ] Point the agent at a branded marketing email id to clone (or say
      "plain" for clean simple HTML)
- [ ] Say GO for workflow build → agent fills campaign.yml with workflow ids
- [ ] Flip `armed: true` (or tell the agent to)
