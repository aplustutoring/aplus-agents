# Bulk messenger config (Roman's rail decisions, 2026-08-14).
portal_id: "6312752"

# BULK ONLY: refuse runs targeting fewer recipients than this.
min_bulk: 25

sms:
  # JustCall from-number routing (Roman 2026-08-14).
  numbers:
    sales: "+18185736644"
    conference: "+18188506284"
    # Roman 2026-09-08 (LOCKED): a family with no deal yet is a LEAD, and lead
    # texts go from the charter_sales seat's line (Paola texts from the sales
    # line; role key per the roles-not-names rule). Once a PO/deal exists the
    # family belongs to scheduling, and scheduling texts come from the support
    # line (email/config.yaml sms.justcall_number). Never the other way round.
    charter_sales: "+18185736644"
    support: "+18188691627"   # scheduling only (families WITH a deal)
  timezone: America/Los_Angeles
  # Live SMS only between these hours (24h, PT) — TCPA-safe window.
  send_window_start: 9
  send_window_end: 20
  per_send_delay_s: 0.5
